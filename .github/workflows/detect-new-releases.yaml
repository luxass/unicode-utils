name: Check Unicode Releases

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
    inputs:
      dry-run:
        type: boolean
        description: Run the workflow without creating a pull request
        required: false
        default: false
      base-url:
        type: choice
        description: the base URL of the ucdjs api
        required: false
        default: "https://api.ucdjs.dev"
        options:
          - "https://api.ucdjs.dev"
          - "https://preview.api.ucdjs.dev"

jobs:
  check-releases:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: write
      pull-requests: write
      models: read
    if: github.repository == 'luxass/unicode-utils'
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: setup pnpm
        uses: pnpm/action-setup@fc06bc1257f339d1d5d8b3a19a8cae5388b55320 # v5.0.0

      - name: setup node
        uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: lts/*
          cache: pnpm

      - name: install dependencies
        run: pnpm install --frozen-lockfile

      - name: check unicode releases
        uses: luxass/unicode-releases-action@5f69f1aa427dda9bf28eb2d545810220fddd4039 # v0.7.5
        id: unicode
        with:
          api_base_url: ${{ github.event.inputs.base-url }}

      - name: generate constants
        env:
          ALL_RELEASES: ${{ steps.unicode.outputs.all_releases }}
          LATEST_RELEASE: ${{ steps.unicode.outputs.latest_release }}
          CURRENT_DRAFT: ${{ steps.unicode.outputs.current_draft }}
        run: |
          node ./packages/metadata/scripts/generate-constants.ts

      - name: format
        run: |
          pnpm oxfmt --write ./packages/metadata/src/index.ts ./packages/metadata/src/data/unicode-version-metadata.ts

      - name: generate diff
        id: generate-diff
        run: |
          # Create a combined diff of index.ts and data files
          {
            echo "=== Changes in src/index.ts ==="
            git diff ./packages/metadata/src/index.ts
            echo -e "\n=== Changes in src/data/ ==="
            git diff ./packages/metadata/src/data/
          } > combined.diff

          # if the diff is empty
          if [ ! -s combined.diff ]; then
            echo "No changes found in the files."
            exit 0
          fi

          echo "Changes found in the files:"
          {
            echo 'DIFF<<EOF'
            cat combined.diff
            echo EOF
          } >> "$GITHUB_OUTPUT"

      - name: generate pr metadata (ai)
        id: pr-metadata
        if: ${{ steps.generate-diff.outputs.DIFF != '' && github.event.inputs.dry-run != 'true' }}
        run: node ./scripts/generate-pr-title.ts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: create pull request
        uses: peter-evans/create-pull-request@c0f553fe549906ede9cf27b5156039d195d2ece0 # v8.1.0
        if: ${{ github.event.inputs.dry-run != 'true' && steps.generate-diff.outputs.DIFF != '' }}
        env:
          DIFF: ${{ steps.generate-diff.outputs.DIFF }}
          COMMIT_MESSAGE: "${{ steps.pr-metadata.outputs.title || 'feat: updated generated files' }}"
        with:
          commit-message: ${{ env.COMMIT_MESSAGE }}
          title: ${{ env.COMMIT_MESSAGE }}
          body: |
            This is an automated PR to update the auto-generated files in the repository.

            <details>
            <summary>📚 Diff</summary><br/>

            ```diff
            ${{ env.DIFF }}
            ```

            <br/></details>

            I will be waiting for your approval 👋.
          branch: update-generated-files
          add-paths: |
            packages/metadata/src/index.ts
            packages/metadata/src/data/unicode-version-metadata.ts
          base: main
          committer: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          author: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          reviewers: luxass
